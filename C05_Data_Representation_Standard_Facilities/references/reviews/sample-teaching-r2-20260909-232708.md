# C05 样章教学复审 r2：APPROVE

审查对象：L14 状态反例修复及其影响面。继承上一轮已通过的 00-06 教学链路审查，仅复核 00-06 hash 未漂移，并重新检查 L14 bad、CMake、README Part 4 与 chapter14 对应段落。

## 结论

APPROVE。上一轮阻断已修复，样章教学门关闭。

## 复验结果

- `chapters/14-bounded-binary-fields.md:34` 到 `:38` 现在只把 bad 根因描述为“过早提交长度头后的 cursor”，不再混入缺 UTF 或缺上限。
- `exercises/L14_binary_fields/validation/bad/bounded_field.hpp:24` 到 `:28` 现在在长度头检查后先执行 `cursor = probe`，再检查 payload 剩余量；截断输入会真实留下外部 `cursor=4`。
- `exercises/L14_binary_fields/CMakeLists.txt:8` 到 `:9` 的 `BAD_DIAGNOSTIC` 已改为 `truncated payload keeps cursor`。
- `exercises/L14_binary_fields/README.md:11` 与 chapter14 叙述一致，明确 bad 保留长度、访问边界与 UTF-8 校验，只故意提前提交 cursor。
- 直接运行 Release bad 可执行，输出为 `check failed: truncated payload keeps cursor`。
- 重新运行 `ctest --test-dir ...verify-core -C Release -R L14_binary_fields_validation_bad_rejected --output-on-failure`，1/1 通过。
- 作者记录的 state-fix 证据显示 verify-debug 13/13、verify-core 13/13、L14 leaf 3/3 通过。

## 继承项

00-06 hash 与上一轮复审后的基线一致，没有新漂移；原先通过的教学链路继续有效：字节/对象表示、端序、整数边界、借用、Unicode 单位、严格转码可以推到 L14 的长度、payload、UTF-8、错误 offset、NUL/BOM、1MiB 预算和游标强保证。

未发现新的 C04 硬先修或下游长度协议断点。

## 审查限制

本轮是样章教学门复审，不替代完整 C05 技术终审、LSP 诊断、ASan/ICU/frontier 验收。
