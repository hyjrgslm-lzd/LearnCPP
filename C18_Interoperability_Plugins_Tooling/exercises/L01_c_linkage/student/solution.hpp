#pragma once
#include <cstddef>
inline int solve(const unsigned char*, std::size_t, unsigned char*) {
    // Implement the ASCII-only transformation; an unfinished operation fails safely.
    return 1;
}
