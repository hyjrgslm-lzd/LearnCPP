cmake_minimum_required(VERSION 3.24)
project(C18_L14_lua_coroutines LANGUAGES C CXX)
include(../cmake/StudySetup.cmake)
if(NOT C18_ENABLE_LUA)
  message(STATUS "C18 L14 Lua disabled; configure with -DC18_ENABLE_LUA=ON and Lua 5.4.9 to build it")
  return()
endif()
include(../L11_lua_stack/LuaSupport.cmake)
c18_lua_exercise(L14_lua_coroutines EXPECT "check failed: owner queue")
