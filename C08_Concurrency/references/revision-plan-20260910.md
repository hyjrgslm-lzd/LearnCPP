# C08 审计与补全实施规格（2026-09-10）

状态：用户已批准实施。依据当前 `LEARNCPP_GLOBAL_PLAN.md` 与 `CONTENT_REFACTORING_GUIDE.md`；对话阶段完成教学、技术调查及独立 code-reviewer、critic 方案审查。本记录不冒充正式 OMX durable consensus 状态。

## 读者、范围与交付

面向能阅读现代 C++、尚未系统掌握并发的工程师。保留既有十八个正文入口、五十个练习与复杂专题，不以目录数量代替内容验收；逐项反查先修、实际契约、正文推导、学生操作、Reference、有效检查、答案、实验、下游使用与非作者审查。正文不使用对话式压缩。

本轮修改范围为 C08 课程及其工具、构建和证据；课外仅根 README、全局计划 7.2 的 C08 状态、C05 第20章的新日志回链。输入指南与其他已有脏文件保留。历史报告、原始测量和用户学习代码不覆盖；新证据绑定新快照。无 Git 提交、推送、关机、凭据或生产系统操作。

新增三条完整教学链：

- U01_async_logging：承接 C05 格式/日志同步前端与 C08 队列/关闭协议，使用既有固定 spdlog 1.17.0 / fmt 12.1.0。局部 pool/logger/sink，受控验证三种溢出策略、线程与 payload 所有权、单/多 worker 顺序、错误处理、flush 请求与完成/持久化的区别、停止生产及排空关停。提供独立学生实现、Reference、good/bad 与逐 Part 解析，不另造完整日志框架。
- F01_thread_attributes：N5054 / P2019R9，名称和栈大小 hint、属性与可调用对象的位置、借用存活、jthread stop_token、零值/平台调整/失败语义。标准接口与平台亲和性分开。
- F02_hazard_pointer_batches：N5054 / P3428R4，make/clear、空/混合 span、empty/nonempty、重复 clear 和强异常保证，加一个保护与退役的集成入口。不加入已删除的 move batch，不把教学 cleanup 或受控模型冒充标准回收接口。

原生 C++26 六设施逐项审计：现有 inplace stop、atomic min/max、SIMD 运行路径复用；标准 HP、RCU、sender 补主体。最小编译/链接能力探测与主体构建/运行分离；DISABLED、能力 SKIP、PASS、主体 FAIL 独立记录。原生接口不可用时仍交付完整源码和实验规格，平台/教学模型另列状态。

## 演进与验证门槛

队列预分配、批量、SPSC 缓存作为代表样章：正确基线→可检验假设→真实计数/受控对照定位→支持范围→已有版本解释→同口径复验。不同线程角色或失败语义单列场景切换。缺证据的方向不得作为已证实优化推进；不预设改进更快。样章非作者通过后再扩展其依赖内容，独立日志/前沿单元可以并行推进。

既有十七组最终样本（其中五组队列）存在；复算并辨认阶段记录、最终采样和后续迁移，修复失效导航。旧样本不回写；新增诊断和受影响正式样本保留全部输出。正式计时每 variant 一次预热、五次独立进程，种子42；记录计时边界、输入、拓扑、编译选项、源码/二进制摘要和负收益。正式采样由主线程安排独占工作窗口，不与本任务编译、其他基准并行。

为十六个原实现型入口保存公开可重放 good/bad 或等价输入脚本，验证初态拒绝、真实学生实现参与、完成体通过、代表行为错误被拒绝、Student-only include 隔离。观察题保留观察语义。复用现有 CTest/runtime/tools，不引入新测试框架。

所有自动运行有进程外超时，覆盖退出和清理。正常、空满、背压、取消、关闭、异常、释放及关键交错按可控场景检查；资源失败明确区分实际注入、模型与未测。有限测试及 Sanitizer 不替代协议推导。

## 构建与本机环境

所有基线/最终验证使用新构建目录，禁止以迁移前缓存或旧可执行文件代替新代码结果。核心默认离线，SPDLOG 扩展默认 OFF，显式源码目录、静态库、external fmt；使用课程自己的小型接线，校验固定提交及完整编译输入，不能直接读取 C05 的 Windows 绝对路径 marker。

Windows 工具解释器明确为 `C:/Users/zhidan.li/AppData/Roaming/uv/python/cpython-3.13.11-windows-x86_64-none/python.exe`（已实测3.13.11及原摘要函数）。保留 runner Python>=3.11 要求，入口提前给出版本诊断；不为默认3.10改哈希算法。Windows 验证 MSVC Debug/Release 核心、既有完整依赖配置、ASan 安全路径及日志扩展。

用户授权在 H 盘创建专用 WSL2 Linux 镜像。目标 `H:\wsl\LearnCPP-C08-Ubuntu-24.04`，发行版同名；使用 Ubuntu 官方24.04.4 AMD64 WSL镜像和校验值。只在新 guest 安装必要官方编译/验证工具，保留镜像复验，不改 Windows 全局配置、已有发行版、默认选择或安全设置。源码快照与构建在 guest ext4，回传纯证据并核对源码摘要。Linux 验证 GCC Debug/Release、Clang ASan/UBSan、独立 TSan 和日志 Release/TSan；Sanitizer 分离构建，TSan先正常/已知竞争控制确认运行时。初始化问题先诊断，环境限制和主体缺陷分别登记。

前沿在两环境分别探测。N1根据实际拓扑判定，多 NUMA 节点缺失保留 SKIP，不模拟实机结论；WSL性能不代替裸机性能。不能强开宏或静默降标准制造通过。

## 分工、审查与停止

主线程负责规格、公共构建/工具、导航、集成验证与最终报告。原生 executor 分别拥有日志、前沿、学生验证资产、演进证据和 WSL 环境范围；跨范围修改先汇报。非作者 code-reviewer/verifier 审查规格/样章、教学遮答案试做、技术及实验结果；作者修复后独立复验，最终跨模块审查绑定文件指纹。

完成条件：旧知识有去向、新增主题闭环齐全、约定可用路径实测通过、所有已知阻断关闭、未测条件及依赖影响明确、导航/计数/版本/原始证据一致、最终独立审查批准。环境限制不伪装成PASS；未完成工作不得仅因已有报告文件而标记完成。
