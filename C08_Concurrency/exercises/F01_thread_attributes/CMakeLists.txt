cmake_minimum_required(VERSION 3.28)
project(F01_thread_attributes LANGUAGES CXX)
include(../cmake/StudySetup.cmake)
cs_add_exercise(F01_thread_attributes OBSERVATION)
foreach(_target F01_thread_attributes F01_thread_attributes_reference)
    if(TARGET ${_target})
        target_compile_definitions(${_target} PRIVATE CS_HAS_STD_THREAD_ATTRIBUTES=${CS_HAS_STD_THREAD_ATTRIBUTES})
    endif()
endforeach()
