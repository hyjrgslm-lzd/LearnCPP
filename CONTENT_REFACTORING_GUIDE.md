# 课程与技术内容重构指引

本指引沉淀 Coroutine 迭代与 Concurrency 重构中确认的工作流程、质量标准和验收约束，供后续课程、专题和配套代码重构使用。它规定怎样完成重构，不固定下一门课程的目录、章节数量或技术选型。

中心标准：**内容全面细致、质量可靠、容易理解、编排合理、由浅入深、逻辑清晰；所有产出接受严格的非作者独立审查。** 目录更整齐、文件更多、代码能编译，都不等于重构完成。

## 1. 范围、授权与优先级

开始前明确读者、目标、重构范围、保留接口、允许的增删改、主验收环境和交付方式。用户说“参考某课程”时，应确认借鉴的是迭代过程、教学质量和验收标准，不能自行缩减成目录模仿。

在已授权范围内，可以按知识依赖新增、合并、拆分、重排和删除内容。旧主题和参考文章都不是不可调整的边界，也不是知识覆盖的上限；增删必须有教学理由和可追踪去向。不得悄悄把完整重构降为只交基础路线。

用户明确约束优先于节省篇幅、减少工作量或工具偏好的建议。采用已有工具、公共组件和标准库，避免与教学无关的框架和依赖；不能因此删掉必要推导、实验、异常处理或审查。

只读分析、实现、提交、推送、系统操作是不同授权层级。后续任务不得因为本指南记录过一次发布或关机，就默认获得相同权限。

## 2. 先勘察历史与现状

不要只看最终文件树。按任务关联性读取参考课程的提交、当前文档与代码，以及用户允许参考的本机会话记录；Codex、OMC 等是互补来源，不因单一来源不完整就推断历史不存在。

形成简短的证据说明：

- 哪些改动解决了什么教学或正确性问题，哪些要求来自用户，哪些只是实现选择。
- 当前课程的知识、练习、答案、组件、构建与实验分别处于什么状态。
- 现有学习代码、未提交改动、暂存区内容及其他协作者修改有哪些，哪些不属于本任务。
- 原计划是否被后续计划替换；以最终获准版本及之后的补充要求为准。

会话只提取与任务有关的约束和经验，不把原始私人记录、凭据或无关路径提交进课程。提交历史可以证明发生了什么，不能冒充无法找到的用户原话。找不到的证据明确标记。

## 3. 建立规格，再组织实施

大规模工作先做并行只读调查：教学结构、代码风险、标准与实现状态可分开调查，由主负责人汇总。随后制定可执行的计划，而不是只有章节名称的清单。

计划至少包含：

- 学习者画像、先修边界、核心问题、知识依赖与完整学习路线。
- 各主题讲解与实现深度，原内容的迁移、合并、删除理由。
- 公共术语、场景、接口、版本命名、实验输入和验收口径。
- 作者批次、独立审查角色、公共文件所有者和依赖关系。
- 构建与运行矩阵、正确性检查、实验和证据保存方式。
- 风险、实际硬件或工具限制、跳过条件、最终完成门槛。

教学规格和实验规格先接受独立审查。选择一个具有代表性的复杂模块制作样章，包含朴素正确基线、问题推导、受控实验、答案和可运行代码；样章经过非作者审查及修复复验后，才作为其他作者的质量基准。不要批量复制尚未验证的结构或错误假设。

## 4. 以知识依赖编排，不以旧目录编排

总体路线由背景知识进入基础概念，再进入底层原理和实际应用。先补后文真正需要的前置概念，避免读者必须先理解答案才能看懂问题。

每个入口明确：

- 为什么现在学，解决什么问题，需要先读什么。
- 当前采用的概念模型、术语和必要背景。
- 从本节能完成什么，哪些内容在后续展开。
- 正文、练习、答案、实验附录和源码导读的相互位置。

复杂模块可以拆成连续系列，每篇仍有完整推导和清晰承接；不能拆成互相依赖却找不到主线的零散笔记。主线与进阶路线只是阅读优先级不同，不是降低进阶内容交付质量的理由。

生产实现源码导读与自行实现分别标注深度。导读必须给出版本、阅读问题、真实入口、关键状态和退出路径，以及与教学方案的对照；只贴项目链接不算覆盖。

## 5. 建立可追踪的覆盖表

按知识点和问题验收，而不是按标题数、题号数或文件数量验收。覆盖记录必须能追踪：

**核心问题 → 先修知识 → 场景与契约 → 演进版本 → 正文和解析 → 代码与实验 → 证据 → 独立审查状态。**

相关信息可以合理分布于主题表、练习说明和质量报告，通过链接形成完整链路；不要为了大表复制多份容易失真的状态。

对原知识点逐项说明保留、迁移、合并或删除的理由；新主题说明补上的知识缺口。合并文档不等于删除知识；保留标题也不等于保留实质内容。验收前检查是否遗漏原有较难主题，以及参考资料没有展开但目标课程需要的方案族。

## 6. 复杂主题采用问题驱动的演进闭环

每个专题先给出场景、输入、接口契约、对象归属和不变量，再建立容易理解、确实正确的基线。

每轮演进必须完成：

1. 提供能构建运行的当前版本及最小使用示例。
2. 提出可检验的预测、通过条件和观察方法。
3. 用具体状态变化、访问方式、反例或测量解释当前问题。
4. 推导改进，解释关键代码为什么需要这样改变。
5. 说明新增状态、所有权、失败路径、成本及适用前提。
6. 继承此前仍适用的检查，补充新不变量对应的检查。
7. 解释结果、局限和剩余问题，自然引向下一步。

每轮集中解释一个主要原因；必须组合改变时，列出依赖，避免读者无法判断收益或问题来自哪里。不能先给复杂成品，再用几句结论代替中间推导。

每条演进关系明确属于“同契约改进”“场景切换”或“独立分支”。换了场景、输入域、顺序或失败保证，要重新建立基线与比较条件，不能画成单一性能升级链。例如，改变队列的生产消费角色并不只是换一种更快实现。

保留基线胜出、优化无收益、结果不稳定及新增成本的真实结果，不以预设“越来越快”作为叙事终点。

## 7. 正文、答案与练习形成闭环

正文应包含背景、连续因果推导、关键代码、必要图示、最小复现命令、观察解释和适用边界。完整工具操作、长配置和原始样本可放附录，但理解核心结论不能依赖读者自行补齐未写出的内容。

不设置人为篇幅上限。不为缩短文本删掉解释，也不以重复清单、术语堆积或无关扩展充当深入。图示只在能更清楚表达状态、依赖、所有权、时序或比较时使用。

所有自测、必做 Part 和关键实验都有完整解析：说明前提、因果、合法结果范围及常见错误推理，不仅给最终数字或代码。

练习逐 Part 说明目标、已提供什么、学生需做什么、独立编辑位置、对应 Reference 和有效检查：

- 实现型 Starter 必须调用学生自己的操作。占位清晰、安全、有限结束；不安全的空实现在线程或危险行为开始前拒绝运行。未完成明确失败，不能输出成功或用 SKIP 代替。
- 观察型起点或实验驱动可以是完整可运行程序，但通过只覆盖实际执行的检查。预测、解释、扩展和多轮实验是否完成，必须另外核对。
- Reference 提供完整正确实现；学生修改不得同时改变自己的参考依据。可以共享输入、公共设施和检查器，但检查器必须实际消费被检验的实现。
- 修改完成标记、直接调用 Reference、恒定返回或预填正确输出，都不能冒充学生实现或算法验证。
- 学生实现、观察运行和 Reference 的测试名称、标签及汇总含义明确区分。

独立教学审查者应遮住答案，沿给定先修和 Starter 尝试完成所审实验，记录未讲先用、缺少步骤、无法复现或必须偷看实现才能继续的位置。

## 8. 技术正确性与证据边界

紧跟当前技术发展，但不把“最新”理解为无条件追逐滚动接口。固定所讨论规范或版本，记录核对日期；区分现行版本、未来版本、提案、实验接口和第三方扩展。

重要且可能变化的结论核对一手来源，包括正式规范、编辑报告、原始算法、官方实现文档或固定版本源码。文章可以启发讲法，不能替代算法与接口验证。

每个实现声明输入和类型前提、容量或资源边界、所有权、状态变化、失败与异常语义、顺序和进展保证。核心算法的性质与包含分配、元素操作、回收等步骤的完整公开操作分别说明。不能以某个底层操作的性质代替完整系统保证。

明确区分：

- 规范要求和允许行为。
- 基于明确假设的协议推导。
- 特定工具链或库的实现细节。
- 当前环境中的实测观察。
- 示意、假设和未验证项。

有限测试、压力运行、工具无报告以及一次漂亮的结果，都不能替代协议推导或证明所有交错正确。交付要求是关闭已知阻断、完成约定验证并诚实界定剩余范围，不能宣称穷尽所有平台与执行情况。

## 9. 代码与构建可复现

先复用已有组件和标准设施，再增加最小必要实现。教学版本需要保留可比较的关键差异；无教学意义的基础工具由单一公共实现承担。

正确性测试和性能驱动复用同一版本的算法。公共接口变化后检查全部消费者，不能只修报告出错的一个调用点。头文件约束、实际调用表达式和文档契约要一致。

提供学生入口、核心验证、完整依赖、可选新特性和性能实验的构建方式，支持单题独立构建。Reference 纳入自动测试；发布构建中的检查不能因优化或禁用调试断言而消失。

核心路径默认离线且不依赖未声明组件。可选依赖固定版本和提交；本地来源、修改状态及无法核实的来源信息如实记录。不静默改用另一依赖或下载版本。

注明最低语言与库要求，同时记录实际生成的编译、链接选项。新能力按头文件、特性宏、真实实例化、链接逐层检查；选项关闭与实际探测失败分开记录。不同标准、实验接口和第三方路径分别验证，不能靠重命名伪装成相同接口。

默认运行安全、有限。危险示例和性能长测显式选择；不自动改变机器全局配置、安装系统组件或运行不支持的硬件路径来制造通过记录。

## 10. 正确性与安全检查

按每个主题的风险制定可运行检查，不能只看总量或退出码。适用时覆盖：

- 正常、边界、空输入、重复调用、容量耗尽和拒绝操作。
- 成功、异常、取消、关闭、重复关闭及已接受工作的最终收束。
- 部分初始化、分配或线程创建失败，异常展开中的资源归属与等待关系。
- 对象存活、独占或借用关系、保护解除、实际释放、重复释放及遗留记录。
- 受控关键交错、历史顺序、版本回绕和短暂状态，而不靠 sleep 猜调度。
- 独立数值参考、运算对应的误差预算、特殊值、有效范围、对齐、别名、尾部和冲突输入。
- 实际执行位置、资源放置、主机能力与安全回退。

为检查器本身保留最小的正例和反例，证明它能拒绝代表性错误。例如，输出缓冲每次用错误哨兵重置，防止“不写结果”的实现沿用上一轮正确值而通过。

所有自动运行均由进程外超时覆盖执行、退出和清理阶段；内部停止标志不能替代它。超时或清理失败是真实失败。测试运行器只终止自己启动的实验进程，并报告未能确认的清理边界。

故意错误的程序与正常测试隔离。真实未定义行为不承诺每次表现一致，优先提供安全的状态模型和受控反例。诊断工具说明适用平台和证据限制；不同检测能力不能互相替代。

长读者、积压或资源耗尽实验设置工作量和内存上限，达到上限停止新增负担并受控收尾，不能让教学演示无限扩张。

## 11. 性能实验与量化结果

先确认正确性，再讨论性能。同组比较统一场景契约、工作量、线程角色、容量、顺序保证、等待方式以及分配和清理责任。固定总工作量与固定每线程工作量分别报告。不同契约不混排，不从不可比结果计算加速比。

使用单调时钟，明确起跑同步和计时区间：初始化、线程创建、实际计算、排空、回收和退出哪些计入，哪些排除。完整比较保持口径一致；有意改变时显式说明。

时间窗口内关闭日志，尽量使用线程局部统计。完整正确性检查单独运行，计时回合仍保留轻量完整性核对。失败回合保留证据，但不得进入有效性能结论。

默认一轮预热、五次独立进程采样，每次重建状态，固定随机种子安排版本顺序。保存所有样本、预热记录、中位数、范围及离散情况，不静默剔除异常值，也不把同进程多行输出冒充多个独立样本。

吞吐按实际成功完成量计算；批量方案报告批量大小及等待代价。尾延迟结论须说明发压和采样方法。库管理且未观察到的线程数标成未知，不能用硬件线程数冒充实际参与数。

覆盖足以检验假设的输入规模和负载差异，不能只挑有利规模。优化无收益、基线胜出、额外开销和结果波动都应解释；不规定固定加速比。

保存完整命令、输入、源码与二进制版本、编译器和标准库、依赖、编译选项、系统与硬件拓扑、计时口径和全部结果。测量期间不修改被绑定的源码或可执行文件；与其他构建或负载重叠时如实说明，必要时保留旧结果另开新组。

平台实验分别记录请求和实际观测。例如，绑定线程的请求不等于页面已经放在目标节点。没有所需硬件、权限或位置证据时明确 SKIP，保留完整正文和实验代码，不虚构数据。局部可运行不代表整个专项通过；部分跳过单独汇总。

## 12. 子任务编排与共享文件所有权

按完整主题演进链分配作者，不按零散段落拆给多个作者再拼凑。每个任务给清楚的读者、输入、写入范围、接口、依赖、交付物和验收要求。

公共接口、构建、基准工具、导航和标准索引由指定负责人维护。作者提出需要的公共变化，由负责人集成；变更后通知并复验所有消费者。并发任务的写入范围不能互相覆盖。

作者完成自检和实际运行后交接，列出修改文件、版本、命令、通过项、失败项和未验证边界。主负责人不把“agent 已完成”自动当作交付通过。

独立审查可以与其他不相干模块的写作并行；被审版本需要可识别，发现阻断则交回作者修复。不要让审查者一边创作同一内容，一边批准自己的实现。

持续维护任务清单，将用户新增要求、发现的缺口、复验和收尾任务一并纳入。上下文切换后依据记录续做，不重启已完成工作，也不遗忘待修复项。

## 13. 独立审查与修复闭环

审查范围包含计划、样章、正文、演进代码、习题、答案、检查器、构建、工具、附录、实验结论、导航、最终报告及通用指引；不能只 review 代码 diff。

审查分三个维度，可由不同非作者承担：

| 维度 | 必须检查 |
|---|---|
| 教学 | 先修、连续推导、覆盖深度、练习可做性、答案解释及阅读路线 |
| 技术 | 规范与版本、算法契约、状态和生命周期、异常路径、数值与访问安全 |
| 实验 | 复现命令、检查有效性、公平比较、干扰、原始数据与结论的支持关系 |

每条发现记录位置、触发条件、依据、根因、影响和修复建议。作者修复后，由非作者复验对应问题及受影响消费者；不能仅把作者“已修复”的回复改成通过。

审查结论绑定具体代码、文档和数据版本，使用提交或文件指纹、实际命令和结果定位。修改后旧结论仍是旧版本证据，不自动继承为新版本批准；原始失败和旧样本不覆盖、不伪装成新结果。

阻断项未关闭，不交付该批。全部批次完成后仍需跨模块集成审查，检查契约一致、先修顺序、导航、测试分类、构建组合和最终报告的真实性。

## 14. 迁移、最终验证与证据交付

更新正文时同步练习说明、答案、公共组件、消费者、构建入口和导航。旧入口优先保留简短迁移说明；必须删除时记录准确范围、理由和替代位置。

最后重新配置、构建和运行约定验证矩阵，而不是汇总过期作者日志。文档链接、锚点和登记检查只证明导航与接线，不证明教学质量；需要与人工独立审查共同使用。

最终质量报告至少列出：

- 实际交付范围、知识迁移及实施深度。
- 精确环境、能力探测和所验证分支。
- 测试、实验及非作者审查结果，代码、文档与数据的版本对应。
- 阻断发现、修复及独立复验闭环。
- PASS、FAIL、SKIP、部分跳过和未验证项的区别与原因。
- 证据位置、复现方式、剩余限制，以及哪些结果只适用于当前环境。

可复现不等于要求下一台机器得到完全相同的耗时；应能重建程序、执行同一协议、解释差异。随仓库交付的证据与仅存在于本机的构建产物明确区分，不能让本地存在的路径伪装成远端可访问的材料。

## 15. 完成门槛与获授权的收尾

宣告重构完成前逐项核对：

- [ ] 最终获准计划和全部新增要求已进入任务清单。
- [ ] 原知识点均有去向，新增内容有必要背景和完整演进。
- [ ] 逐 Part 的任务、独立作业位置、Reference、有效检查和解析相互对应。
- [ ] 所有目标代码与文档完成约定验证，已知阻断清零并经非作者复验。
- [ ] 性能结论有完整原始证据，不可比或失败样本未被用于排名。
- [ ] 无法在本机验证的条件明确记录，没有伪造通过或悄悄删去内容。
- [ ] 跨模块导航、构建、接口、实验口径与报告一致。
- [ ] 通用经验已沉淀；用户数据及无关改动得到保留。

若本次任务另有提交或推送授权：检查暂存区，只提交任务所属文件；排除私人记录、凭据、无关改动和无需发布的构建产物。非强推地发布到明确远端与分支，核对远端提交确实包含本次交付；不能以本地 commit 成功代替 push 成功。

若本次任务另有退出、关机等授权：只在全部前置任务和发布核验成功后执行，采用不会强制丢弃未保存数据的方式。发布失败、验证阻断或必需选择未解决时，不提前执行后置系统操作。

这些收尾动作按每次任务的实际授权决定，不是本指南赋予的默认权限。

## 本指南的来源与维护

本指南依据参考课程的实际迭代、2026-09-08 获准的 Concurrency 最终计划、用户后续补充及本轮独立审查发现整理。参考课程的代表性迭代包括补齐参考检查、修正所有权语义、重排教学正文和补足前置知识；借鉴的是解决问题的方法，不是照抄目录。

本次具体交付与证据见 [Concurrency 课程](Concurrency_Study/README.md)及[质量报告](Concurrency_Study/references/quality-report.md)。后续项目替换自己的目录、规范版本、技术清单和环境，但保留上述流程与质量约束。需要更改约束时写明原因和验收影响，并接受独立审查。
