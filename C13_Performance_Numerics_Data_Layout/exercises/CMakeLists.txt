cmake_minimum_required(VERSION 3.28)
project(C13_Performance_Numerics_Data_Layout LANGUAGES CXX)
include(cmake/StudySetup.cmake)
foreach(lesson L01_stable_reduction L02_layout L03_tiled_matrix L04_particle_pipeline
    L05_measurement_observation L06_random_views L07_simd_parallel L08_numa)
  add_subdirectory(${lesson})
endforeach()
if(C13_ENABLE_THIRD_PARTY)
  add_subdirectory(L10_units)
  add_subdirectory(L11_stdblas)
endif()
if(C13_ENABLE_FRONTIER)
  add_subdirectory(F01_native)
endif()
get_property(students GLOBAL PROPERTY C13_STUDENT_TARGETS)
add_custom_target(c13_students DEPENDS ${students})
