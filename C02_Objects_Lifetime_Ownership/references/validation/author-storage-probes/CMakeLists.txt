cmake_minimum_required(VERSION 3.28)
project(c02_storage_probes LANGUAGES CXX)

function(configure_probe target)
    target_compile_features(${target} PRIVATE cxx_std_23)
    set_target_properties(${target} PROPERTIES CXX_EXTENSIONS OFF CXX_SCAN_FOR_MODULES OFF)
    if(MSVC)
        target_compile_options(${target} PRIVATE /utf-8 /EHsc /W4 /permissive- /Zc:__cplusplus)
    else()
        target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic)
    endif()
endfunction()

foreach(target IN ITEMS env_report implicit_move range_for_lifetime_macro start_lifetime_as)
    add_executable(${target} probes/${target}.cpp)
    configure_probe(${target})
endforeach()
