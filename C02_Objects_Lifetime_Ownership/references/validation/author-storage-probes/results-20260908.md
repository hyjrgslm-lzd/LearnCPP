# C02 storage probe results, 2026-09-08

Command:

```powershell
python Core_Study/references/validation/author-storage-probes/run_probes.py
```

Fresh evidence:

```text
Core_Study/references/validation/author-storage-probes/evidence/storage-probes-20260908-220803.json
```

Environment:

- CMake 4.2.3.
- MSVC generator: `Visual Studio 18 2026`, `-A x64`.
- MSVC tools: `D:/VisualStudio2026/Installed/VC/Tools/MSVC/14.51.36231`.
- CMake detected MSVC compiler version: `19.51.36256.0`.
- Clang: `D:/VisualStudio2026/Installed/VC/Tools/Llvm/x64/bin/clang++.exe`, version `22.1.3`.
- Clang was launched from a child `VsDevCmd` environment captured by `build/c02-storage-probes/capture-vsdev-env.cmd`; no global environment was modified.
- The archived JSON includes SHA256 fingerprints for the probe sources and captured CMake, Clang, and MSVC `cl /Bv` version output.

Status matrix:

| Probe | MSVC C++23 / VS18 | Clang 22.1.3 `-std=c++23` |
|---|---:|---:|
| `env_report` build/run | PASS | PASS |
| `implicit_move` build/run | PASS | PASS |
| `range_for_lifetime_macro` build/run | PASS | PASS |
| `start_lifetime_as` build/run | PASS | FAIL |

Key outputs:

- MSVC `env_report`: `_MSC_VER=1951`, `__cplusplus=202400`, `_MSVC_LANG=202400`, `__cpp_implicit_move=202207`, `__cpp_range_based_for=202211`, `__cpp_lib_start_lifetime_as=202207`.
- Clang `env_report`: `__clang_major__=22`, `__clang_minor__=1`, `__clang_patchlevel__=3`, MSVC-compatible `_MSC_VER=1951`, `__cplusplus=202302`, `_MSVC_LANG=202302`, `__cpp_implicit_move=202207`, `__cpp_range_based_for=202211`, `__cpp_lib_start_lifetime_as=not-defined`.
- MSVC `start_lifetime_as`: printed `value=0x12345678` and `first_byte=0x78`.
- Clang `start_lifetime_as`: compile failed at the intentional `#error "__cpp_lib_start_lifetime_as is not provided by this standard library"`.

Frontier compile-only checks:

| Probe | MSVC `/std:c++latest` | Clang `-std=c++2c` |
|---|---:|---:|
| C++29 base-class designated initializer shape | FAIL | FAIL |
| C++26 return temporary reference rejection shape | PASS with warning | FAIL |

Boundaries:

- `range-for` lifetime is macro-only here. The probe does not execute a dangling range.
- Provenance, invalid-pointer, lifetime-end DRs, erroneous initialization behavior, and defaulted assignment wording were not treated as runtime-probed.
- ASan was not rerun in this lane.
