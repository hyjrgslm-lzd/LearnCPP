cmake --list-presets -S Core_Study/exercises => PASS from repo root, available configure presets: verify-core, verify-debug, student, asan, frontier
cmake --build --list-presets from Core_Study/exercises =>
Available build presets:

  "verify-core"
  "verify-debug"
  "student"
  "asan"
  "frontier"

ctest --list-presets from Core_Study/exercises =>
Available test presets:

  "verify-core"
  "verify-debug"
  "student"
  "asan"
  "frontier"

