cmake_minimum_required(VERSION 3.28)
project(c02_asan_probe LANGUAGES CXX)
include("${CMAKE_CURRENT_LIST_DIR}/../../../exercises/cmake/StudySetup.cmake")
add_executable(c02_asan_safe probe.cpp)
core_configure_target(c02_asan_safe)
core_add_test(c02_asan_safe LABELS capability)
if(CORE_STUDY_ENABLE_UNSAFE_DEMOS)
    add_executable(c02_asan_fault probe.cpp)
    target_compile_definitions(c02_asan_fault PRIVATE C02_INTENTIONAL_FAULT=1)
    core_configure_target(c02_asan_fault)
endif()
