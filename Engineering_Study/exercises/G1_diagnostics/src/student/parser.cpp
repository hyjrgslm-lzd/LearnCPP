#include "parser.hpp"

int parse_two_digits(std::string_view text) {
    (void)text;
    // TODO: Validate that text has exactly two decimal digits, then return 0..99.
    // This starter deliberately fails the first checker without indexing invalid input.
    return -1;
}
