cmake_minimum_required(VERSION 3.24)
project(c06_g2_doc_probe LANGUAGES CXX)
add_executable(g2_doc_probe g2_doc_probe.cpp)
if(MSVC)
  target_compile_options(g2_doc_probe PRIVATE /std:c++latest /permissive- /W4)
else()
  target_compile_options(g2_doc_probe PRIVATE -std=c++2c -Wall -Wextra -Wpedantic)
endif()
enable_testing()
add_test(NAME g2_doc_probe COMMAND g2_doc_probe)
